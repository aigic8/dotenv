# Neovim Config

## Tasks

- [ ] use `lualine` with `trouble` so the status line would be red when there is
  an error found in the `trouble`
- [ ] in `trouble` list only show the errors and not the other items
